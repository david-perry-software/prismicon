```yaml
status: in-progress
branch: feature/custom-variant-authoring
last-updated: 2026-09-11
next-step: "4.4 Add README ## Custom variants"
initiative: "scalable-icon-variants"
```

## Phase 1: Baseline and failing tests

- [x] 1.1 Confirm the recorded baseline on this branch before touching any file — verify: `npm ci && npm test 2>&1 | grep -E "^# (tests|pass|fail)"` prints `# tests 86`, `# pass 86`, `# fail 0`; `git diff --quiet origin/main -- src index.d.ts README.md demo test package.json` exits 0; `npx -y -p typescript tsc --noEmit --strict --target es2020 --lib es2020,dom --types "" index.d.ts 2>&1 | grep -cE "error TS"` prints `1` and that line names `'react'`
- [x] 1.2 Add `test/authoring.test.js` (node:test, JSDOM harness copied from `test/renderer-dispatch.test.js` with globals restored `afterEach`) covering plan.md `## Approach` items 1–3: `validateVariant` accepts `square` and every id of the built-in registry (`BUILT_IN_VARIANTS.ids.map(get)`), returns a frozen descriptor, and rejects with `TypeError` matching `/Variant "bad".*determinism.*paint/` a copy of `square` whose `paint` appends `Math.random()`, `/Variant "bad".*"derive".*"window"/` one whose `derive` reads `globalThis.window.innerWidth`, `/"describe"/` one returning `''`, `/"flash"/` one returning `42`; `globalThis` descriptors for `window, document, navigator, matchMedia, requestAnimationFrame` are `deepEqual` before and after a passing and a failing SSR probe; `createPrismicon()` with no args lists exactly the built-in ids and its `renderStaticSVG(seed)` equals the module-level `renderStaticSVG(seed)` for each `STATIC_SEEDS` entry and each built-in id; `createPrismicon({ variants: [square] })` is frozen, lists `[...builtInIds, 'square']`, `renderStaticSVG('maya', { variant: 'square' })` contains `<rect`, `mountGlyph(container, 'maya', { variant: 'square', state: 'working' })` changes the `<rect transform` after one frame; `{ variants: [{ ...square, id: 'polyhedron' }] }` throws `/Duplicate prismicon variant id/`; `{ builtIns: false, variants: [square] }` throws `/Default variant "polyhedron" is not registered/` and `{ builtIns: false, variants: [square], defaultId: 'square' }` lists `['square']`; `{ variants: [randomPaint], validate: false }` does not throw; `{ variants: 'nope' }` throws `TypeError`; root exports `Object.keys(await import('../src/index.js')).sort()` equal the current list plus `createPrismicon, createVariantRegistry, defineVariant, validateVariant, VARIANT_ID_PATTERN` — commit before any `src/` change — verify: `node --test test/authoring.test.js; echo "exit=$?"` prints `exit=1` with every case `not ok` (imports of `../src/authoring.js` / `../src/variants/validate.js` fail); `git diff --quiet origin/main -- src` exits 0
- [x] 1.3 Extend `test/react-variant.test.js` with the provider cases from plan.md `## Acceptance checklist`: SSR parity inside `PrismiconProvider` against `instance.renderStaticSVG`; the same tree without a provider throws `RangeError`; client mount inside the provider renders a `<rect>` (`act` + `createRoot`); replacing the `registry` prop with a second `createPrismicon({ variants: [square] }).registry` remounts (different `<svg>` node) while keeping `state`; `registry={{}}` throws `TypeError` via `onUncaughtError`; and change the export assertion to `['Prismicon', 'PrismiconProvider', 'default']` — verify: `node --test test/react-variant.test.js 2>&1 | grep -E "^# (tests|fail)"` shows `# fail` ≥ 5 with only the new cases and the export case `not ok`; `git diff --quiet origin/main -- src` exits 0

## Phase 2: Validation and factory

- [x] 2.1 Add `src/variants/validate.js` exporting `validateVariant(descriptor, { seeds, size, states } = {})` per plan.md `## Approach` item 1 (shape via `defineVariant`, output contract, determinism probe over `STATIC_SEEDS`/both `dark` values/every state + `'settling'` + one `animate` step with `dt: 1/60`, SSR probe redefining configurable browser globals with throwing getters and restoring descriptors in `finally`), with the probed state list as a local `PROBE_STATES` constant (`core.js` imports `variants/index.js`, so importing `STATES` back would create a cycle) that `test/authoring.test.js` asserts `deepEqual` to `[...STATES, 'settling']`, and re-export it from `src/variants/index.js` — verify: `node --test test/authoring.test.js 2>&1 | grep -E "^# (tests|fail)"` shows all `validateVariant` and globals-restoration cases passing (remaining failures are `createPrismicon`/export cases only); `node -e "import('./src/variants/validate.js')"` exits 0
- [x] 2.2 Add `src/authoring.js` exporting `createPrismicon({ variants = [], defaultId = DEFAULT_VARIANT_ID, builtIns = true, validate = true } = {})` per plan.md `## Approach` item 2, returning `Object.freeze({ registry, renderStaticSVG, mountGlyph, listVariants })` built from `createRenderer(registry)`; add `createPrismicon`, `defineVariant`, `createVariantRegistry`, `validateVariant`, `VARIANT_ID_PATTERN` to `src/index.js` — verify: `node --test test/authoring.test.js 2>&1 | grep -E "^# (tests|fail)"` prints `# fail 0`; `npm test 2>&1 | grep -E "^# fail"` prints `# fail 0` except the pending `test/react-variant.test.js` cases; `git diff --quiet origin/main -- src/core.js src/variants/registry.js src/variants/polyhedron.js src/variants/ncube.js` exits 0

## Phase 3: React provider

- [x] 3.1 Update `src/react.js` per plan.md `## Approach` item 4: `createContext(null)`, exported `PrismiconProvider({ registry, children })` that throws `TypeError('PrismiconProvider "registry" must be a variant registry')` unless `registry` has `resolve`, `get` and `ids`, memoises `{ registry, ...createRenderer(registry) }` on `[registry]`, and provides it; `Prismicon` reads the context, falls back to a module-level frozen `{ registry: BUILT_IN_VARIANTS, renderStaticSVG, mountGlyph }`, resolves `variant` through `ctx.registry.resolve`, uses `ctx.renderStaticSVG` / `ctx.mountGlyph`, and adds `ctx` to the mount-effect deps — verify: `node --test test/react-variant.test.js 2>&1 | grep -E "^# (tests|fail)"` prints `# fail 0`; `npm test 2>&1 | grep -E "^# (tests|fail)"` prints `# fail 0` and `# tests` ≥ 110; `grep -c "PrismiconProvider" src/react.js` ≥ 2; `head -1 src/react.js` prints `'use client';`

## Phase 4: Types, example, docs

- [x] 4.1 Extend `index.d.ts` per plan.md `## Approach` item 5 (`VariantFlash`, `VariantAnimateContext`, `VariantPaintEffects`, `VariantDescriptor`, `VariantRegistry`, `ValidateVariantOptions`, `CreatePrismiconOptions`, `PrismiconInstance`, `defineVariant`, `validateVariant`, `createVariantRegistry`, `createPrismicon`, `VARIANT_ID_PATTERN`; React `PrismiconProviderProps`, `PrismiconProvider`) with one-line JSDoc per hook naming when the engine calls it and the reduced-motion rule — verify: `for s in VariantDescriptor VariantRegistry PrismiconInstance validateVariant createPrismicon defineVariant createVariantRegistry VARIANT_ID_PATTERN PrismiconProvider PrismiconProviderProps; do grep -c "$s" index.d.ts; done` prints no `0`; `npx -y -p typescript tsc --noEmit --strict --target es2020 --lib es2020,dom --types "" index.d.ts 2>&1 | grep -cE "error TS"` prints `1` and that line names `'react'`
- [x] 4.2 Create `demo/square-variant.js` as the consumer-style example (`import { defineVariant } from '../src/index.js'`, `export const square = defineVariant({ ... })`, `export const SPEC_VERSION`, the hooks as named exports as today), replace the body of `test/fixtures/square-variant.js` with `export * from '../../demo/square-variant.js';` plus the existing header comment — verify: `npm test 2>&1 | grep -E "^# fail"` prints `# fail 0`; `grep -c "defineVariant" demo/square-variant.js` ≥ 2; `npm pack --dry-run 2>&1 | grep -cE "demo/|test/"` prints `0`
- [x] 4.3 Update `demo/index.html` per plan.md `## Approach` item 6: import `createPrismicon` and `../demo/square-variant.js` → `./square-variant.js`, build `const prismicon = createPrismicon({ variants: [square] })` at module scope, fill the hero `<select>` from `prismicon.listVariants()`, mount the hero and Dimensions gallery via `prismicon.mountGlyph`, add a `<h2>Custom variant</h2>` section mounting `square` at `idle`, `working`, `done` with a one-paragraph pointer to README `## Custom variants` — verify: `grep -c "createPrismicon\|square-variant.js\|Custom variant" demo/index.html` ≥ 4; `node -e "import('./demo/square-variant.js').then(m => console.log(Object.isFrozen(m.square)))"` prints `true`
- [ ] 4.4 Add README `## Custom variants` between `## Variants` and `## Derivation spec v1 (frozen)` per plan.md `## Approach` item 7 (descriptor contract table with the eight hook signatures and call timing, reduced-motion = idle `paint`, `spec` versioning, id pattern; `validateVariant` probes and error shape; `createPrismicon` options incl. duplicate/shadowing rule and hoisting; `PrismiconProvider` usage and remount-on-change; the square example excerpt) — verify: `grep -n "^## " README.md` shows `## Variants`, `## Custom variants`, `## Derivation spec v1 (frozen)` consecutively in that order; `grep -c "createPrismicon\|validateVariant\|PrismiconProvider" README.md` ≥ 6; `grep -c "defineVariant" README.md` ≥ 1

## Phase 5: Gate and hand-off

- [ ] 5.1 Merge `origin/main` (merge, never rebase), then run the complete gate (policy §5 — no lint configured, so the gate is the full suite plus the ad-hoc checks): `npm ci && npm test 2>&1 | grep -E "^# (tests|fail)"` prints `# fail 0` and `# tests` ≥ 110; `node scripts/generate-golden.mjs && git diff --quiet -- test/fixtures` exits 0; `git diff --quiet origin/main -- src/core.js src/variants/registry.js src/variants/polyhedron.js src/variants/ncube.js` exits 0; `npm pack --dry-run 2>&1 | grep -E "src/|index.d.ts|test/|demo/"` lists `src/authoring.js`, `src/variants/validate.js`, `index.d.ts` and nothing under `test/` or `demo/`; the `index.d.ts` compile check from 4.1 still prints `1` — verify: every command exits 0 and outputs match
- [ ] 5.2 Browser check of the demo — verify: `local:3115` — run `python3 -m http.server 3115 --directory .` in the worktree, open `http://localhost:3115/demo/index.html`, confirm the hero `<select>` lists `Square` after the built-ins and selecting it mounts a spinning square, the Custom variant section shows three squares (`idle`, `working`, `done`), the console shows no errors, and the Dimensions gallery still renders 3-cube…6-cube; save the screenshot as `features/2026/09/custom-variant-authoring/evidence/step-5-2-demo-custom-variant.png`, link it from this step, and stop the server
- [ ] 5.3 Set roadmap `status: in-review`, `next-step: ""`, tick this step, commit and push — verify: `git status --porcelain` is empty and `git log origin/feature/custom-variant-authoring -1 --format=%s` shows the roadmap commit
